# 架构总览（完整业务骨架）

> **项目**：tribunal-order-audit-system — B2B 订单审单系统（DDD + 微服务形态）
> **定位**：上游（物料+金额）→ 中游（订单审单）→ 下游（金融+履约）的完整业务闭环，全部模块合于同一项目
> **形态**：10 个模块（9 业务模块 + 1 共享库），逻辑域独立（模块化微服务）、部署合一（同一工程）
> **技术栈**：Spring Boot 3.2.x / Spring Cloud OpenFeign / MyBatis-Plus / MySQL（每模块独立库）/ JDK 21

---

## 一、模块全景（10 模块）

```
tribunal-order-audit-system
│
├── tribunal-common/               # 共享库（非微服务）：ApiResponse / BizException / 跨服务 DTO
│
├── 上游（前置：物料 + 金额）
│   ├── tribunal-inventory-service/     # 库存物料域：主数据/库存账/预占释放/推送上游
│   └── tribunal-marketing-service/     # 营销价格域：价格体系/促销/折扣/押金引擎
│
├── 中游（核心交易）
│   ├── tribunal-auth-service/          # 认证授权域：用户/角色/登录/RBAC
│   ├── tribunal-customer-service/      # 客户信用域：客户/信用组/信用额度
│   └── tribunal-order-service/         # 订单审单域：下单/状态机/审单/五合一编排
│
├── 下游（后置：金融 + 履约）
│   ├── tribunal-billing-service/       # 金融账单域：账单生成/审核/结算/核销/回传
│   └── tribunal-fulfillment-service/   # 履约执行域：出库/发货/签收/发送工厂
│
└── 横贯（支撑能力）
    ├── tribunal-notification-service/  # 通知域：站内信/邮件/短信/微信
    └── tribunal-task-service/          # 定时任务域：超时关单/状态对账/数据归档
```

## 二、服务清单（端口 / 独立库）

| 模块 | 职责 | 端口 | 独立库 |
|---|---|---|---|
| tribunal-auth-service | 登录/注册/角色权限（RBAC） | 8087 | tribunal_auth |
| tribunal-customer-service | 客户/信用组/信用额度 | 8081 | tribunal_customer |
| tribunal-order-service | 下单/状态机/审单/编排中心 | 8080 | tribunal_order |
| tribunal-inventory-service | 物料主数据/库存账/预占释放 | 8083 | tribunal_inventory |
| tribunal-marketing-service | 价格体系/促销/折扣/押金 | 8084 | tribunal_marketing |
| tribunal-billing-service | 账单生成/审核/结算/核销/回传 | 8082 | tribunal_billing |
| tribunal-fulfillment-service | 出库/发货/签收/发送工厂 | 8085 | tribunal_fulfillment |
| tribunal-notification-service | 站内信/邮件/短信/微信 | 8086 | tribunal_notification |
| tribunal-task-service | 超时关单/对账/归档 | 8088 | tribunal_task |
| tribunal-common | 共享代码（非独立运行） | - | - |

## 三、业务三大块闭环

```
【上游】拿物料 + 金额
  inventory（物料/库存/预占）───┬─── marketing（价格/促销/押金）
                                │
【中游】订单审单                 ▼
  auth（登录鉴权）→ customer（信用）→ order（下单/审单/状态机）
                                │
【下游】金融处理 + 履约 + 发工厂  ▼
  billing（账单/结算/核销）→ fulfillment（出库/发货/签收/发送工厂）
                                │
【横贯】                        ▼
  notification（通知）＋ task（超时关单/对账/归档）
```

## 四、order-service 五合一编排链路（核心）

```
审单通过(order-service)
  ① 信用校验      → customer-service（可用信用 ≥ 应付）
  ② 取价校验      → marketing-service（客户价→客户组价→区域价，上游"金额"）
  ③ 预占库存      → inventory-service（逐 SKU 预占，防超卖）
  ④ 生成账单      → billing-service（账单 GENERATED，转单）
  ⑤ 创建履约      → fulfillment-service（履约 GENERATED，待出库/发货）
  ⑥ 发送通知      → notification-service（站内信给经销商）
  ⑦ 状态迁移      → order 状态机：待确认 → 已确认
```

**账单/履约回传**：
- billing 结算/核销 → 回传 order → 订单状态机推进（转单中 → 已转单）
- fulfillment 发货/签收 → 回传 order → 订单终态（已发货/已签收）

## 五、SQL 清单（9 份，各服务独立库）

| SQL 文件 | 库 | 核心表 |
|---|---|---|
| sql/auth.sql | tribunal_auth | t_user / t_role |
| sql/customer.sql | tribunal_customer | t_customer |
| sql/order.sql | tribunal_order | t_order / t_order_sku / t_order_status_record |
| sql/inventory.sql | tribunal_inventory | t_inventory_item / t_inventory_flow |
| sql/marketing.sql | tribunal_marketing | t_price_rule / t_promotion_rule / t_deposit_rule |
| sql/billing.sql | tribunal_billing | t_bill / t_bill_line / t_bill_payment |
| sql/fulfillment.sql | tribunal_fulfillment | t_fulfillment_order / t_fulfillment_line / t_factory_order |
| sql/notification.sql | tribunal_notification | t_notification_message / t_notification_template |
| sql/task.sql | tribunal_task | t_task_log |

## 六、本地启动与联调

```bash
# 1. 建库建表（9 份 SQL 按序执行）
mysql -uroot -p < sql/auth.sql
mysql -uroot -p < sql/customer.sql
mysql -uroot -p < sql/order.sql
mysql -uroot -p < sql/inventory.sql
mysql -uroot -p < sql/marketing.sql
mysql -uroot -p < sql/billing.sql
mysql -uroot -p < sql/fulfillment.sql
mysql -uroot -p < sql/notification.sql
mysql -uroot -p < sql/task.sql

# 2. 修改各服务 application.yml 数据库账号密码

# 3. 启动顺序（依赖下游先行）
mvn -pl tribunal-auth-service spring-boot:run          # 8087
mvn -pl tribunal-customer-service spring-boot:run      # 8081
mvn -pl tribunal-inventory-service spring-boot:run     # 8083
mvn -pl tribunal-marketing-service spring-boot:run     # 8084
mvn -pl tribunal-billing-service spring-boot:run       # 8082
mvn -pl tribunal-fulfillment-service spring-boot:run   # 8085
mvn -pl tribunal-notification-service spring-boot:run  # 8086
mvn -pl tribunal-task-service spring-boot:run          # 8088
mvn -pl tribunal-order-service spring-boot:run         # 8080（最后，编排中心）

# 4. 联调验证（建议用例）
# ① 登录取 Token：POST /api/auth/login
# ② 物料入库：POST /api/inventory/items
# ③ 价格配置：POST /api/marketing/price
# ④ 下单：POST /api/orders（状态=待确认）
# ⑤ 审单：POST /api/orders/{id}/review（触发五合一编排）
# ⑥ 账单结算：POST /api/bills/{id}/settle（回传订单状态机）
# ⑦ 履约发货/签收：POST /api/fulfillments/{id}/ship、/sign
# ⑧ 查通知：GET /api/notifications/{id}
```

## 七、学习演进路线（生产化）

| 里程碑 | 主题 | 关键动作 |
|---|---|---|
| M2 收尾 | 主干道完整 | 信用占用/释放（F-403）、Feign 重试熔断（N-207）、审单权限 |
| M3 | 异步与事件 | 引入 MQ、库存变动流水（F-504）、库存推送（F-505）、收款流水（F-606）、财务对账（F-607） |
| M4 | 业务规则引擎 | 促销/折扣/押金配置化、规则引擎 |
| M5 | 微服务治理 | Nacos 注册中心、网关、熔断降级、分布式锁 |
| M6 | 可观测与运维 | 日志/链路/监控/告警/CI-CD、压测 |
| M7 | 生产加固 | 分库分表、归档、容灾、安全加固 |

> 详细需求编号与代码映射见《需求编号与代码实现映射表》，业务名词见《业务名词与业务处理解析》，目标见《需求规格说明书-生产级目标》。
