# 需求编号与代码实现映射表

> **文档定位**：把《需求规格说明书-生产级目标》中的需求编号（F-功能 / N-非功能 / Q-工程质量）映射到
> tribunal-order-audit-system 代码中的**类/方法/TODO**，标注实现状态与所属里程碑。
> **用途**：按图索骥——查需求编号 → 找到对应代码位置 → 看 TODO 逐条实现。
> **状态图例**：✅ 已实现 / 🟡 部分实现 / ⬜ 未实现（TODO）

---

## 一、功能需求映射（F-xxx）

### 3.1 商品与价格域

| 需求 | 需求描述 | 代码位置 | 状态 | 里程碑 |
|---|---|---|---|---|
| F-101 | SKU 管理 | ⬜ 无 SKU 领域（未来 sku-service） | ⬜ | M4 |
| F-102 | 价格体系 | ⬜ 无价格领域（OrderSku.price 为下单传入） | ⬜ | M4 |
| F-103 | 运费计算 | `ShippingFeeCalculator`（满额免邮 ≥500 / 基础运费 + 每件加价，封顶 50）+ `OrderApplicationService.createOrder` 未指定时自动计算 | ✅ | M4 |
| F-104 | 渠道价映射 | `OrderApplicationService.createOrder`（TODO：渠道校验） | 🟡 | M4 |

### 3.2 营销域

| 需求 | 需求描述 | 代码位置 | 状态 | 里程碑 |
|---|---|---|---|---|
| F-201 | 促销配置 | ⬜ 无营销服务（未来 marketing-service） | ⬜ | M4 |
| F-202 | 促销计算 | `OrderSku` 构造器（金额=数量×单价）+ `OrderApplicationService` L68（TODO：促销/折扣重算） | 🟡 | M4 |
| F-203 | 折扣池 | `Customer` / `CreditLimit`（字段未含折扣池余额） | 🟡 | M4 |
| F-204 | 折扣池抵扣 | `CreditLimit.occupy/release`（TODO） | 🟡 | M4 |
| F-205 | 押金管理 | ⬜ 无押金字段（OrderSku TODO：deposit） | ⬜ | M4 |
| F-206 | 折扣上限 | `DiscountCapPolicy`（促销/手动折扣 ≤ 商品总额×50%，超限截断）+ `OrderApplicationService` 促销折扣应用前截断 | ✅ | M4 |

### 3.3 订单域（★核心）

| 需求 | 需求描述 | 代码位置 | 状态 | 里程碑 |
|---|---|---|---|---|
| F-301 | 下单 | `OrderApplicationService.createOrder` → `Order.create` → `orderRepository.save` | ✅ | M1 |
| F-302 | 整托校验 | `OrderReviewDomainService.validateWholePallet`（规格由参数传入）+ `OrderSku` 数量校验 | ✅ | M1 |
| F-303 | 信用校验 | `OrderReviewDomainService.validateForReview`（Feign 查信用）+ `OrderApplicationService` L58（TODO） | 🟡 | M2 |
| F-304 | 订单状态机 | `OrderStatus`（枚举 + TRANSITIONS 静态块）+ `Order.transitTo` | ✅ | M1 |
| F-305 | 状态流水 | `OrderStatusRecordPo` + `OrderStatusRecordMapper` + `OrderStatusRecordListener`（订阅事件写流水） | ✅ | M1 |
| F-306 | 审单 | `OrderReviewApplicationService.review`（approve/reject）→ `Order.confirm/reject` | ✅ | M2 |
| F-307 | 转单 | `Order.startTransfer/transferSuccess`（方法已定义，调用未接） | 🟡 | M2 |
| F-308 | 状态回传 | ⬜ 无账单回传消费（未来 MQ/Feign 回调） | ⬜ | M3 |
| F-309 | 改单 | ⬜ 无改单用例 | ⬜ | M3 |
| F-310 | 拼车 | `CarPoolGroup` 聚合 + `CarPoolGroupStatus` 状态机 + `CarPoolApplicationService`（发起/加入/确认/关闭/取消）+ `CarPoolController`（POST /api/car-pools）+ 确认时联动 `Order.joinCarPool()`（已拼车不可关闭） | ✅ | M4 |
| F-311 | 空包装回收 | ⬜ 无回收领域（OrderCreateCommand TODO） | ⬜ | M4 |
| F-312 | 预购 | ⬜ 无预购类型（OrderStatus 无 PRE_ORDER 分支） | ⬜ | M4 |

### 3.4 客户与信用域

| 需求 | 需求描述 | 代码位置 | 状态 | 里程碑 |
|---|---|---|---|---|
| F-401 | 客户管理 | `Customer` 聚合 + `CustomerRepository` + `CustomerApplicationService`（仅查信用） | 🟡 | M0 |
| F-402 | 信用组 | ⬜ 无信用组（CreditLimit 单值） | ⬜ | M2 |
| F-403 | 信用占用/释放 | `CreditLimit.occupy/release` + `CustomerApplicationService.occupyCredit/releaseCredit` + 审单通过占用/拒绝·取消释放（已接入 `OrderReviewApplicationService`/`OrderApplicationService`，F-403） | ✅ | M2 |
| F-404 | 收款确认 | ⬜ 无收款流程 | ⬜ | M2 |

### 3.5 库存与履约域

| 需求 | 需求描述 | 代码位置 | 状态 | 里程碑 |
|---|---|---|---|---|
| F-501 | 物料主数据 | `InventoryItem` 聚合 + `InventoryApplicationService.upsert` + `InventoryController POST /api/inventory/items` | ✅ | M2 |
| F-502 | 库存查询 | `InventoryApplicationService.getBySkuCode` + `InventoryController GET /api/inventory/items/{skuCode}`（可售量 = total - reserved） | ✅ | M2 |
| F-503 | 库存预占/释放 | `InventoryItem.reserve/release` + `InventoryApplicationService` + `InventoryController` + `OrderReviewApplicationService.approve`（审单时逐 SKU 预占） | ✅ | M2 |
| F-504 | 库存变动流水 | `InventoryFlowPo`/`InventoryFlowMapper` + `InventoryApplicationService.recordFlow`（reserve/release/returnStock 落库 t_inventory_flow） | ✅ | M3 |
| F-505 | 库存推送上游 | ⬜ 无（TODO：变化事件 → MQ/Feign 推送订单服务） | 🟡 | M3 |
| F-506 | 发货/签收回传 | `Order.ship/sign`（订单状态机已支持） | 🟡 | M3 |

### 3.6 财务审核域（下游金融计算）

| 需求 | 需求描述 | 代码位置 | 状态 | 里程碑 |
|---|---|---|---|---|
| F-601 | 账单生成 | `FinanceBill.generate` + `BillingApplicationService.generateBill` + `BillController POST /api/bills`（order 审单通过时 Feign 转单） | ✅ | M2 |
| F-602 | 账单审核 | `FinanceBill.confirm` + `BillingApplicationService.confirm` + `BillController POST /api/bills/{id}/confirm` | ✅ | M2 |
| F-603 | 收款结算 | `FinanceBill.settle` + `BillingApplicationService.settle` + `BillController POST /api/bills/{id}/settle` | ✅ | M2 |
| F-604 | 账务核销 | `FinanceBill.verify` + `BillingApplicationService.verify` + `BillController POST /api/bills/{id}/verify` | ✅ | M2 |
| F-605 | 账单状态回传 | `BillStatusChangedEvent` + `BillStatusCallbackListener`（Feign 回传）→ `BillStatusCallbackApplicationService`（订单状态机驱动） | ✅ | M2 |
| F-606 | 收款流水 | `BillPaymentPo`/`BillPaymentMapper` + `BillingApplicationService.recordPayment`（settle 落库 t_bill_payment） | ✅ | M3 |
| F-607 | 财务对账 | `PaymentReconcileTask`（billing-service 定时核对账单金额 vs 收款流水，缺失自动补记、金额不一致落 t_reconcile_record） | ✅ | M3 |

### 3.7 通知与消息域

| 需求 | 需求描述 | 代码位置 | 状态 | 里程碑 |
|---|---|---|---|---|
| F-701 | 站内信 | ⬜ 无（OrderCreatedEvent 已发布，无订阅者） | 🟡 | M3 |
| F-702 | 邮件通知 | ⬜ 无 | ⬜ | M3 |
| F-703 | 短信/微信 | ⬜ 无 | ⬜ | M3 |
| F-704 | 模板管理 | ⬜ 无 | ⬜ | M3 |

### 3.8 对账与报表域

| 需求 | 需求描述 | 代码位置 | 状态 | 里程碑 |
|---|---|---|---|---|
| F-801 | 状态对账 | `OrderBillStatusReconcileTask`（order-service 定时核对审单通过订单的账单存在性，差异落 t_reconcile_record） | ✅ | M3 |
| F-802 | 消息对账 | `OutboxReconcileTask`（order-service 定时核对 outbox FAILED/超时 PENDING，FAILED 自动重置重投、差异落 t_reconcile_record） | ✅ | M3 |
| F-803 | 销售报表 | ⬜ 无 | ⬜ | M6 |

### 3.9 系统管理域

| 需求 | 需求描述 | 代码位置 | 状态 | 里程碑 |
|---|---|---|---|---|
| F-901 | 用户/角色/权限 | `User`/`Permission` 领域 + `AuthApplicationService`（登录/注册/刷新/登出）+ `AuthInterceptor`（接口级鉴权）+ `@RequirePermission` 注解 + `t_role`/`t_permission`/`t_role_permission` 表 | ✅ | M5 |
| F-902 | 操作日志 | ⬜ 无（GlobalExceptionHandler TODO） | ⬜ | M5 |
| F-903 | 参数配置 | ⬜ 无配置中心 | ⬜ | M5 |

---

## 二、非功能需求映射（N-xxx）

| 需求 | 需求描述 | 代码位置 | 状态 | 里程碑 |
|---|---|---|---|---|
| N-101/102 | RT 指标 | ⬜ 无压测 | ⬜ | M6 |
| N-103/104 | QPS 指标 | ⬜ 无压测 | ⬜ | M6 |
| N-105 | 消息吞吐 | ⬜ 无 MQ | ⬜ | M3 |
| N-201 | 服务可用性 | ⬜ 单实例 | ⬜ | M5 |
| N-203 | 消息不丢 | ⬜ 无 MQ | ⬜ | M3 |
| N-204 | 缓存防护 | ⬜ 无 Redis 缓存 | ⬜ | M4 |
| N-205 | 幂等 | `OrderStatus.canTransitTo`（状态机守卫）+ `t_order.order_no` 唯一约束 + `OrderRepositoryImpl.save`（按 orderNo 查重） | ✅ | M1 |
| N-207 | 超时与重试 | `FeignRetryConfig`（Retryer 重试 3 次）+ `@EnableRetry` + `spring-retry` 依赖（N-207） | ✅ | M2 |
| N-301 | 下单+信用预占 | `OrderApplicationService.createOrder`（@Transactional，TODO：信用预占） | 🟡 | M2 |
| N-302 | 库存最终一致 | `InventoryItem.reserve/release`（预占/释放）+ 对账 TODO（最终一致） | 🟡 | M3 |
| N-303 | 转单最终一致 | `BillingFeignClient.transfer`（生成账单）+ 失败重试 TODO | 🟡 | M2 |
| N-304 | 状态回传幂等 | `OrderStatus` 状态机守卫 + `BillStatus` 状态机守卫（重复状态拒绝） | ✅ | M1 |
| N-305 | 通知可靠 | ⬜ 无通知 | ⬜ | M3 |
| N-401 | 认证 | `JwtProvider`（JWT 双 Token：Access+Refresh）+ `LoginFailGuard`（失败锁定防爆破）+ `RefreshTokenStore`（轮换/吊销）+ BCrypt 密码加密 | ✅ | M5 |
| N-402 | 授权 | RBAC（`t_permission`/`t_role_permission` 权限点）+ `AuthInterceptor`（`@RequirePermission` 接口级鉴权）+ `UserContext`（ThreadLocal 当前用户）+ `FeignInternalTokenInterceptor`（服务间互信） | ✅ | M5 |
| N-403 | 数据校验 | `@Valid` + `OrderCreateRequest/OrderReviewRequest` + `GlobalExceptionHandler`（MethodArgumentNotValidException） | ✅ | M0 |
| N-405 | 防重复提交 | ⬜ 无（orderNo 唯一键已兜底） | 🟡 | M4 |
| N-406 | 审计 | `OrderStatusRecordListener`（状态流水即审计）+ `GlobalExceptionHandler`（TODO：异常落 ES） | 🟡 | M5 |
| N-501/502/503 | 可观测性 | ⬜ 无（Logback 默认） | ⬜ | M6 |
| N-504 | 告警 | ⬜ 无 | ⬜ | M6 |
| N-601 | 数据量预估 | ⬜ 无 | ⬜ | M7 |
| N-602 | 分库分表 | ⬜ 无 | ⬜ | M7 |
| N-605 | 唯一键 | `t_order.order_no` / `t_customer.customer_code` / `t_inventory_item.sku_code` / `t_bill.source_order_no` 唯一索引 | ✅ | M0 |
| N-701 | 水平扩展 | 🟡 服务已无状态化设计（Feign url 直连待换 Nacos） | 🟡 | M5 |
| N-703 | 配置化 | ⬜ 无 | ⬜ | M4 |

---

## 三、工程质量映射（Q-xxx）

| 需求 | 需求描述 | 代码位置 | 状态 | 里程碑 |
|---|---|---|---|---|
| Q-101 | 分层不违规 | 领域层纯 Java（Order/OrderStatus/OrderId 无 Spring 依赖）；架构测试 ⬜ | 🟡 | M0 |
| Q-102 | 命名规范 | ✅ 包名/类名统一（com.demetrius.tribunal.*） | ✅ | M0 |
| Q-201 | 核心规则单元测试 | `OrderStatusTest`（11 用例）+ `OrderTest`（8 用例） | ✅ | M1 |
| Q-202 | 集成测试 | ⬜ 无 | ⬜ | M2 |
| Q-203 | 契约测试 | ⬜ 无 | ⬜ | M2 |
| Q-204 | 端到端测试 | ⬜ 无 | ⬜ | M3 |

---

## 四、TODO 快速索引（按文件）

> 每个 TODO 的完整实现要点见对应文件头注释，此处为「需求 → TODO → 位置」速查。

| 文件 | TODO 要点 | 对应需求 |
|---|---|---|
| `OrderApplicationService` | 校验客户/SKU/整托/渠道（L58）；促销重算（L68）；业务异常（L85）；订单 ID/编号生成（L92/L100） | F-301/F-302/F-303/F-202 |
| `OrderReviewApplicationService` | 审单权限/重计价/信用扣减（Feign 重试已接入 `FeignRetryConfig`） | F-306/F-303/N-207 |
| `OrderReviewDomainService` | 促销折扣重算（L50）；权限校验（L51）；整托规格来源（L71） | F-202/F-306 |
| `Order` | 金额计算抽取（L59）；事件发布（L175）；信用校验 | F-202/F-304 |
| `OrderSku` | 押金/折扣前金额（L12）；金额扩展（L43） | F-205/F-202 |
| `OrderRepositoryImpl` | 明细更新策略（L57）；乐观锁 | N-205/N-602 |
| `GlobalExceptionHandler` | 异常落 ES（L52） | N-406 |
| `InventoryApplicationService` | 库存变动流水（F-504）；推送上游（F-505）；乐观锁防超卖 | F-504/F-505/N-602 |
| `InventoryItemRepositoryImpl` | 乐观锁；库存流水落库 | N-602/F-504 |
| `BillingApplicationService` | 生成账单幂等查重；金额核对（F-607）；回传失败重试 | F-601/F-607/N-303 |
| `BillRepositoryImpl` | 账单状态/时间戳还原；乐观锁 | F-601/N-602 |
| `BillStatusCallbackListener` | 回传失败重试（记录待重传 + 定时补偿） | F-605/F-801 |
| `MybatisPlusConfig` | 乐观锁 @Version（L13） | N-602 |
| `AuthApplicationService` | 验证码/登录审计（双 Token/失败锁定/RBAC 已实现） | F-901/N-401 |
| `LoginFailGuard` / `RefreshTokenStore` | 单实例内存实现，生产换 Redis（多实例共享锁定/吊销） | N-401 |
| `AuthInterceptor` / `JwtTokenParser` | 数据权限（部门级）预留；生产密钥走配置中心 | N-402/F-901 |

---

## 五、已实现能力小结（✅ 清单）

| 能力 | 覆盖需求 | 验证 |
|---|---|---|
| DDD 四层架构（common/customer/order/inventory/billing 五模块） | Q-101/Q-102 | `mvn clean test` ✅ |
| 下单用例（校验→聚合→保存→事件） | F-301 | OrderApplicationService |
| 订单状态机（合法/非法/重复迁移） | F-304/N-304 | OrderStatusTest 11 用例 |
| 整托校验 + 金额校验 | F-302 | OrderReviewDomainService |
| 审单 + 三合一编排（信用→库存预占→账单生成） | F-306/F-303/F-503/F-601 | OrderReviewApplicationService |
| 跨服务信用校验（Feign） | F-303 | CustomerFeignClient |
| 库存物料（主数据/查询/预占释放） | F-501/F-502/F-503 | InventoryApplicationService |
| 金融账单（生成/审核/结算/核销 + 状态机） | F-601~F-605 | BillingApplicationService + FinanceBill |
| 账单状态回传驱动订单状态机 | F-605/N-304 | BillStatusCallbackListener + BillStatusCallbackApplicationService |
| 状态流水落库 | F-305/N-406 | OrderStatusRecordListener |
| 幂等（状态机守卫 + 唯一键） | N-205/N-605 | OrderTest/建表 SQL |
| 全局异常处理 + 参数校验 | N-403 | GlobalExceptionHandler（三服务） |
| 认证（BCrypt + JWT 双 Token + 失败锁定 + 刷新轮换/登出） | F-901/N-401 | AuthApplicationService + JwtProvider + LoginFailGuard + RefreshTokenStore |
| 授权（RBAC 权限点 + 接口级鉴权 + UserContext + Feign 内部互信） | F-901/N-402 | AuthInterceptor + @RequirePermission + FeignInternalTokenInterceptor |
| 信用占用/释放闭环（审单通过占用，拒绝/取消释放） | F-403 | OrderReviewApplicationService + OrderApplicationService |
| Feign 失败重试 | N-207 | FeignRetryConfig + @EnableRetry |
| 订单状态机补全（SPLITTING/SPLITTED/PARTIALLY_SHIPPED/PARTIALLY_SIGNED） | F-304 | OrderStatus TRANSITIONS + OrderStatusM4Test 11 用例 |
| 寻源分仓（按 SKU 贪心选最大库存仓） | F-304 | WarehouseRoutingService + WarehouseRoutingServiceTest 7 用例 |
| 拆单（按仓拆子单 + 金额按比例分摊 + 父单状态聚合） | F-304 | OrderSplitService + Order.aggregateChildStatus + OrderSplitServiceTest 8 用例 + OrderStatusAggregateTest 10 用例 |
| 单元测试（M1/M2 46 + M4 新增 35 = 81 用例） | Q-201 | surefire 报告 |
| 业务文档 + 需求文档 + 映射表 + 认证设计文档 | - | docs/ 四份 |

---

## 六、学习路径建议（从当前状态出发）

1. **已完成 M0（架构底座）+ M1（订单核心闭环）+ M2（三合一编排 + 收尾）**：
   下单/状态机/整托/流水/库存预占/账单生成/回传/信用占用释放（F-403）/Feign 重试（N-207）均已就位
2. **已完成认证授权链路（auth-service + 全服务鉴权接入）**：
   登录/注册/双 Token/失败锁定/RBAC 接口鉴权（F-901/N-401/N-402）
3. **已完成 M4 核心三项（状态机补全 + 寻源分仓 + 拆单）**：
   OrderStatus 新增 4 个状态及迁移边；WarehouseRoutingService 按 SKU 贪心选仓；
   OrderSplitService 按仓拆子单 + 金额按比例分摊；Order.aggregateChildStatus 父单状态聚合（35 个新增单元测试）
4. **再下一步 M3**：引入 MQ（N-203）、库存变动流水落库（F-504）、库存推送上游（F-505）、
   收款流水（F-606）、财务对账（F-607）、领域事件改消息
5. **再下一步 M4 剩余**：合单逻辑、促销引擎、优惠券、押金引擎、售后退货
4. 每个里程碑完成后，回到本表把对应行的「状态」改为 ✅，形成自己的进度清单
